# Changelog - Wake 5

## 阶段

第 5 次唤醒：改进阶段。

## 改进内容

1. 新增 `docs/raid_basics.md`，作为 Level 0 Python 模型和 RAID 基础概念之间的桥。
2. 在主 README 的“当前可运行入口”中加入 `docs/raid_basics.md`，并直接给出 demo + pytest 命令。
3. 更新 `TODO.md`，标记 `docs/raid_basics.md` 已完成。
4. 更新 `notes/progress_log.md`，记录本轮小闭环。

## 下一步留给检验阶段的问题

- `docs/raid_basics.md` 是否足够准确、清楚、有趣；
- 主 README 的入口是否更适合新读者；
- 下一轮是否该补 `docs/raid0_mapping.md`，还是先给 demo 增加 RAID5 降级读取可视化。
